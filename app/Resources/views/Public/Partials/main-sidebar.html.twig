<div class="col-md-3">
    <p class="lead">Shop Name</p>
    <div class="list-group">
        {% for categorie in categories %}
            <a href="{{ path('app.categorie',{"id":categorie.id}) }}" class="list-group-item">{{ categorie.title }}</a>
        {% endfor %}

    </div>
</div>